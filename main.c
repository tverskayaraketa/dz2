#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <locale.h>
#include "func.h"

int main()
{
    setlocale(LC_ALL, "Russian");

    //char* str = malloc(sizeof(int));
    //gets(str);
    //ex_1(str);
    //puts(str);

    //ex_2();

    //ex_3();

    return 0;
}
